#!/usr/bin/env bash

## REF: https://spack.readthedocs.io/en/latest/getting_started.html

WORKSPACE=${WORKSPACE:?required}

cd $WORKSPACE/lib/

[ -d spack/.git ] && (cd spack && git pull) ||  git clone --depth 1  https://github.com/spack/spack.git

#SPACK_PYTHON=

