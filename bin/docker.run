#!/usr/bin/env bash
set -x

## PURPOSE: generic wrapper for "docker run"
## REF: https://docs.docker.com/engine/reference/run/

. $WORKSPACE/etc/docker/run.properties

ARG=${ARG:-$1}

: ... starting container 

docker run $OPTIONS $IMAGE $COMMAND $ARG

#docker run \
#--name $NAME \
#-w $WORKDIR \
#--mount "${MOUNT}" \
#$TTY_MODE $IMAGE $RUN_CMD
